# Configuration du serveur
PORT=3001
NODE_ENV=development

# JWT Secret (changez ceci en production)
JWT_SECRET=eugene-store-secret-key-change-in-production

# Base de données (optionnel pour le moment)
# DATABASE_URL=mongodb://localhost:27017/eugene-store
# DATABASE_URL=postgresql://user:password@localhost:5432/eugene-store

# Configuration des uploads
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880

# Configuration email (optionnel)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Configuration paiement (optionnel)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_... 